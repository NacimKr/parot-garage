{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Contact</h1>
        {{form_start(form)}}
            {{form_widget(form)}}
        {{form_end(form)}}
    </div>
{% endblock %}